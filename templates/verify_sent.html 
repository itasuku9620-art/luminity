{% extends "base.html" %}
{% block title %}メール確認が必要です{% endblock %}
{% block content %}
<main style="max-width:520px;margin:24px auto;padding:0 16px;">
  <h1>メール確認が必要です</h1>
  <p>
    {{ email }} に認証メールを送信しました。<br>
    メール内のリンクをクリックして認証を完了してください。
  </p>

  <form method="post" action="{{ url_for('verify_resend') }}" style="margin-top:12px;">
    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
    <input type="hidden" name="email" value="{{ email }}">
    <button class="btn" type="submit">認証メールをもう一度送る</button>
    <a class="btn ghost" href="{{ url_for('login') }}" style="margin-left:8px;">ログインへ戻る</a>
  </form>
</main>
{% endblock %}
